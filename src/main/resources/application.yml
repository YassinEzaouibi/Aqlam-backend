spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/platform-aqlaam
    username: postgres
    password: yasso
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect